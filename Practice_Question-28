//Write a function to calculate percentage of a student from marks in science,maths,sanskrit.
#include <stdio.h>
int calcPercentage(int science,int maths,int sanskrit);
int main() {
    int science=98;
    int maths=95;
    int sanskrit=99;
    printf("Percentge is:%d",calcPercentage(science,maths,sanskrit));
    return 0;
}
    int calcPercentage(int science,int maths,int sanskrit){
        return ((science+maths+sanskrit)/3);
    }
